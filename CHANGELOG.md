# 새벽봇 변경 이력

버전별 변경사항 기록.

## 1.2.0
- 룰렛 기능 추가 (등록한 이름과 정확히 일치할 때만 반응, 후원 문구/일반 명령어 중 선택, 항목별 확률(%) 지정과 확률 고정, `!목록추가`로 채팅에서 항목 추가)
- 룰렛 결과를 방송 화면에 표시하는 오버레이 기능 추가 (OBS 브라우저 소스, 회전 휠/슬롯머신 선택, 계정 연동 탭에서 설정)
- 계정 연동 탭에 데이터 백업/복원 기능 추가 (치지직 인가 토큰은 제외)
- 유저 명령어/룰렛 표에서 사용 횟수 표시 제거, 필요 권한·쿨타임·활성 칸 가운데 정렬
- 룰렛 추가/편집 창 UI 정리 (토글 한 줄 정리, 쿨타임 항목 통합, 창 크기 자동 제한, 확률 소수점 2자리 제한)
- 룰렛 결과 확률 자동 분배 로직 개선 (더 균등하게 분배되고, 항목 삭제 시에도 재분배)
- 설명 아이콘(?) 풍선말이 화면 밖으로 잘리거나 흔들리던 문제 수정
- Redirect URI 입력 칸이 저장 전에도 기본값으로 채워져 있던 문제 수정 (직접 저장하기 전까진 빈칸으로 표시)
- 계정 연동 탭 카드 순서 정리 (오버레이를 위로, 봇 연결 상태·데이터 백업을 아래로)
- 룰렛 오버레이 스핀 애니메이션에 반동 효과 추가, 시작 즉시 표시되도록 개선
- 유저 명령어에 "! 없이 반응" 옵션 추가 (채팅이 명령어 이름과 정확히 일치할 때만 반응)
- `!핑`/`!업타임`/`!명령어` 응답 문구를 대시보드 편집 버튼으로 수정 가능하도록 개선
- 안 쓰는 파일(overlayStore.js) 제거, 룰렛 추가/수정 API 검증 로직 중복 제거

## 1.1.0
- `!출첵` 출석체크 명령어 추가 (연속/총/최장 출석 기록, 방송 중일 때만 동작)
- 연속 출석은 달력상 날짜가 아닌 실제 방송이 있었던 날 기준으로 계산
- 대시보드에 출석체크 탭 추가 (프로필/닉네임/유저 식별코드, 검색, 기록 삭제)
- 출첵 기능 켜고 끄는 토글 추가 (꺼도 기존 기록은 유지)
- 전체 수정: 날짜 선택 후 기존 출첵 이력이 있는 유저 전체에 일괄 출석/결석 반영
- 개인 수정: 유저별 달력에서 특정 날짜 출석 여부를 직접 수정
- 출석 기록도 다른 데이터와 동일하게 %APPDATA%에 저장
- 관리 페이지를 3초 폴링 대신 실시간 이벤트(SSE) 방식으로 변경

## 1.0.5
- 관리 페이지 XSS로 계정 토큰 탈취 가능하던 취약점 수정
- 자기 메시지 필터가 시간 제한 없이 무한정 유지되던 버그 수정
- 명령어/금칙어/토큰 저장을 임시 파일 교체 방식으로 변경해 저장 중 강제종료 시 손상 방지
- 명령어 예약어 검사, 잘못된 쿨타임 값 기본값(3초) 처리 추가
- 코드 주석 전체 정리

## 1.0.4
- 윈도우 11 최적화 (작업표시줄/알림 인식 개선, Mica 배경)
- 안 쓰는 윈도우 서비스 등록 스크립트(node-windows) 제거
- 설정 저장 API 방어 코드 보강

## 1.0.3
- 창 닫기 확인창에 "다음부터 기억하기" 옵션 추가
- .env를 %APPDATA%로 이동 + asar 패키징으로 시작 속도 개선
- 유틸리티 파일 4개를 utils.js로 통합
- 명령어 사용 횟수 저장 디바운스 처리
- 금칙어 검사용 소문자 캐싱
- (중요) 유틸리티 통합 후 실행 불가 버그 수정
- 설정 파일 손상 시 백업 후 초기화
- GitHub Actions로 exe 빌드 + Release 자동 배포 구축

## 1.0.2
- 자기 메시지 필터링 개선 (본계정 인가 시에도 명령어 정상 동작)
- 계정 연동 탭에 버전 표시
- .env가 배포 패키지에 포함되지 않도록 수정

## 1.0.1
- 이름을 "새벽봇"으로 통일
- 명령어 반영 지연, 소켓 중복, 토큰 갱신 경쟁 상태 버그 수정
- Redirect URI 직접 입력 기능 추가

## 1.0.0
- 치지직 공식 Open API 기반 첫 버전 (OAuth 인가, 채팅 연결, 기본/커스텀 명령어, 관리 화면)
