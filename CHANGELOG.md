# 새벽봇 변경 이력

버전별 변경사항을 기록하는 문서입니다. README는 "지금 버전 사용법"만 다루고,
버전이 올라갈 때마다 무엇이 바뀌었는지는 여기 계속 쌓아둡니다. 태그(`v1.0.4` 등)를
붙여서 푸시하면, 이 문서에서 해당 버전 항목만 자동으로 뽑아서 GitHub Release
설명으로 사용됩니다.

## 1.0.4
- **윈도우 11 최적화**
  - 작업표시줄/알림 센터에서 이 앱이 하나의 프로그램으로 정확히 인식되도록 앱
    ID를 명시적으로 지정했습니다. (예전에는 이 값이 없어서 알림에 "Electron"
    같은 엉뚱한 이름이 뜨거나, 작업표시줄 아이콘이 따로 노는 경우가 있을 수
    있었습니다.)
  - 윈도우 11(빌드 22000 이상)에서 실행하면 창 배경에 윈도우 11 기본 앱들과
    비슷한 반투명 Mica 효과가 적용됩니다. 윈도우 10에서는 이 효과 자체가
    지원되지 않아서, 자동으로 예전과 똑같은 불투명 배경이 그대로 유지됩니다.
- 지금 쓰는 Electron(트레이) 방식과 맞지 않고 안내 문구도 예전(네이버 쿠키 로그인 시절)
  내용 그대로 남아있던 "윈도우 서비스 등록" 스크립트(`scripts/install-service.js`,
  `scripts/uninstall-service.js`)와 관련 `node-windows` 의존성을 정리했습니다. 실제로
  쓰이지 않는 기능이었고, 안내 문구의 포트 번호도 실제와 달라 혼동을 줄 수 있었습니다.
- 설정 저장 API가 값이 문자열이 아닐 때도 안전하게 처리하도록 방어 코드를 보강했습니다.

## 1.0.3
- 창 닫기(X 버튼) 확인창에 "다음부터 이 설정 기억하기" 체크박스를 추가했습니다.
- `.env` 파일을 설치 폴더가 아니라 `%APPDATA%\saebyeokbot` 폴더로 옮기고, 앱
  코드를 압축 아카이브 하나(asar)로 묶어서 시작 속도를 개선했습니다.
- 서로 관련 없는 작은 유틸리티 파일 4개를 `utils.js` 하나로 합쳤습니다.
- 커스텀 명령어의 "사용 횟수" 저장을 짧게 모아서 한 번에 처리하도록 바꿔,
  디스크 쓰기 횟수를 줄였습니다.
- 금칙어 검사 시 매번 하던 소문자 변환을 등록/로드 시점에 한 번만 하도록
  캐싱해서 검사 속도를 개선했습니다.
- (중요) 실행이 아예 안 될 수 있던 버그를 잡았습니다 — 유틸리티 통합 과정에서
  최상위 `index.js`가 예전 경로를 참조하고 있던 것을 발견해 수정했습니다.
- 커스텀 명령어/금칙어/시스템 명령어 설정 파일이 손상됐을 때 조용히 빈 값으로
  초기화되던 것을, 손상된 파일을 백업해두고 시작하도록 바꿨습니다.
- GitHub Actions로 exe 자동 빌드 + 버전 태그 푸시 시 GitHub Release 자동 배포를
  구축했습니다.
- README 예시 문구와 코드 주석에 섞여 있던 다른 상용 챗봇 관련 표현을 모두
  새로 쓴 문구로 교체했습니다.

## 1.0.2
- 봇이 자기 메시지를 걸러내는 방식을 개선했습니다. 기존에는 봇을 인가한
  계정과 같은 계정이 보낸 메시지를 전부 무시했는데, 이제는 "봇이 실제로 보낸
  메시지와 내용이 정확히 같은 경우"만 걸러내서, 본계정으로 인가해도 방송인
  본인이 직접 친 명령어가 정상 동작합니다.
- 관리 페이지 "계정 연동" 탭에 현재 프로그램 버전을 표시했습니다.
- `.env` 파일이 배포 패키지에 포함되지 않도록 해서, 업데이트할 때마다 이미
  설정해둔 값이 덮어써지는 문제를 없앴습니다.

## 1.0.1
- 이름을 "새벽봇(공식판)"에서 "새벽봇"으로 통일했습니다.
- 커스텀 명령어를 채팅으로 추가해도 관리 화면에 바로 반영되지 않던 문제,
  재연결 시 소켓이 중복 생성되던 문제, 토큰 갱신 중 경쟁 상태 문제 등을
  수정했습니다.
- Redirect URI를 화면에서 직접 입력/저장할 수 있게 했습니다.

## 1.0.0
- 치지직 공식 Open API를 사용하는 첫 버전. OAuth 인가, 채팅 연결, 기본
  명령어(`!핑`/`!업타임`/`!명령어`), 커스텀 명령어 등록/수정/삭제, 관리용
  데스크톱 앱 화면을 구현했습니다.
